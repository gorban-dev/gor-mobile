---
description: Using Git Worktrees — isolate feature work in a separate worktree before /plan or /implement
---

Task from user: **$ARGUMENTS**

## gor-mobile overlay (two deltas only)

This command runs the superpowers `using-git-worktrees` skill **verbatim** — read
`~/.claude/skills/gor-mobile-using-git-worktrees/SKILL.md` and follow it exactly.

The following two adaptations apply (they ADD to, do not override, the skill):

### 1. Architecture rules + examples
No rule load required — this command only manages git worktrees, not code.

### 2. Local-LLM delegation
This command routes to Opus — no local delegation.
