# Stability

<!-- STAGE 2 (book distillation): stable vs unstable types, @Stable /
     @Immutable contracts, collection parameters and
     kotlinx.collections.immutable, how instability defeats skipping.
     Own wording + own Kotlin examples. -->
