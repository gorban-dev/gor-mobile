# Side effects in Compose

<!-- STAGE 2 (book distillation): LaunchedEffect, DisposableEffect,
     rememberCoroutineScope, SideEffect, produceState, snapshotFlow — when
     each applies, lifecycle, common misuse. Own wording + own Kotlin
     examples. -->
