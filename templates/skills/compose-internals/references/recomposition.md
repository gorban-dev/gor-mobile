# Recomposition & skipping

<!-- STAGE 2 (book distillation): what triggers recomposition and how it is
     scoped; skipping conditions; restartable scopes; key(...); positional
     memoization in practice. Own wording + own Kotlin examples. -->
